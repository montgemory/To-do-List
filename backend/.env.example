# 应用配置
PORT=3002
NODE_ENV=production

# 数据库配置 - 本地开发
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=todolist

# 数据库配置 - Railway（部署时使用）
# DATABASE_URL=postgres://user:password@host:port/dbname

# 前端URL（更改为你的Vercel应用URL）
FRONTEND_URL=https://your-vercel-app.vercel.app 